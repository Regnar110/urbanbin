import './contactsectionheader.scss'

const ContactSectionHeader = ({sectionTitle}) => {
    return (
        <div className='contact-section-header'>{sectionTitle}</div>
    )

}

export default ContactSectionHeader